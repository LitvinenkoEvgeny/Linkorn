mixin banner(items)
    section.banner
        .container
            each item in items
                .banner__item
                    h3.banner__item-text=item.text
                    p.banner__item-value=item.value

